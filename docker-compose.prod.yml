# docker-compose.prod.yml

services:
  django_gunicorn:
    build:
      context: .
      dockerfile: Dockerfile.prod  # Dockerfile.prod 파일을 사용하여 빌드
    expose:
      - "8000"
    env_file:
      - ./src/.env  # .env 파일 경로를 src 폴더 내로 지정
    environment:
      - POSTGRES_DB=oz_main
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_HOST=db

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 5
    depends_on:
      - db

  db:
    image: postgis/postgis:15-3.3
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: oz_main
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 30s
      timeout: 10s
      retries: 5

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 30s
      timeout: 10s
      retries: 5

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./resources/nginx/prod-nginx.conf:/etc/nginx/conf.d/default.conf  # 배포용 Nginx 설정 파일 연결
      - ./frontend/build:/usr/share/nginx/html
    depends_on:
      - django_gunicorn
#      - frontend
#
#  frontend:
#    build:
#      context: ./frontend  # 프론트엔드 코드가 있는 디렉토리 경로
#      dockerfile: Dockerfile.prod  # 프론트엔드 Dockerfile
#    volumes:
#      - frontend_data:/usr/share/nginx/html  # 프론트엔드 빌드 파일 공유
#    expose:
#      - "3000"
#    environment:
#      - NODE_ENV=production

volumes:
  postgres_data:
#  frontend_data:
